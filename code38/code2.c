#include <stdio.h>

int main()
{
    int a = 0, b = 0;
    long long int sum = 0, gap = 0;
    sum = a + b;
    gap = a - b;
    long long int x = a * b;
    int q = a / b; //除法取商
    int r = a % b; //除法取余
}