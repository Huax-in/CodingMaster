C

printf()

    文件操作

    IOCTL/DRM

    驱动程序

C语言调用静态库
    创建 math.c 文件
    编译 clang -c math.c -o math.o
    创建 main.c 文件
        extern关键字
    编译 clang main.c math.o

屏幕像素点和RGB三原色

HAL(硬件抽象层)

显卡文件路径
    /dev/dri